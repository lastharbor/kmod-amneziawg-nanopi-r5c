diff --git a/src/compat/compat.h b/src/compat/compat.h
index 516f2e6..f1b210f 100644
--- a/src/compat/compat.h
+++ b/src/compat/compat.h
@@ -1223,7 +1223,7 @@ static inline void dst_cache_reset_now(struct dst_cache *dst_cache)
 #define genl_info_dump(cb) genl_dumpit_info(cb)
 #endif
 
-#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 1, 84) && \
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 1, 200) && \
 	!(LINUX_VERSION_CODE >= KERNEL_VERSION(4, 19, 312) && LINUX_VERSION_CODE < KERNEL_VERSION(4, 20, 0)) && \
 	!(LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 274) && LINUX_VERSION_CODE < KERNEL_VERSION(5, 5, 0)) && \
 	!(LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 215) && LINUX_VERSION_CODE < KERNEL_VERSION(5, 11, 0)) && \
@@ -1288,7 +1288,7 @@ static inline void dev_sw_netstats_rx_add(struct net_device *dev, unsigned int l
 }
 #endif
 
-#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 1, 91) && !defined(ISUBUNTU2004) && !defined(ISUBUNTU2204) && !defined(ISRHEL9)
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 1, 200) && !defined(ISUBUNTU2004) && !defined(ISUBUNTU2204) && !defined(ISRHEL9)
 #include <linux/timer.h>
 static inline int timer_delete(struct timer_list *timer)
 {
